# 自建 Raydium 池子项目 - 环境变量配置模板
# 复制此文件为 .env 并填入你的实际配置

# Solana RPC 端点
# 推荐使用 Helius 或其他高性能 RPC 提供商
DEVNET_RPC_URL=https://api.devnet.solana.com
# 或者使用 Helius: https://devnet.helius-rpc.com/?api-key=YOUR_API_KEY

# 钱包配置
# 钱包私钥文件路径 (JSON 格式)
WALLET_FILE_PATH=./devnet-wallet.json
# 或者使用系统默认路径: ~/.config/solana/id.json

# 钱包公钥 (用于验证)
WALLET_PUBLIC_KEY=YOUR_WALLET_PUBLIC_KEY_HERE

# 项目配置
# 是否启用详细日志
VERBOSE_LOGGING=true

# 交易配置
# 交易确认级别 (processed, confirmed, finalized)
COMMITMENT_LEVEL=confirmed

# 最大重试次数
MAX_RETRIES=3

# 交易超时时间 (秒)
TRANSACTION_TIMEOUT=60

# 测试配置
# 测试用的代币数量
TEST_SOL_AMOUNT=0.01
TEST_USDC_AMOUNT=18

# 滑点容忍度 (百分比)
SLIPPAGE_TOLERANCE=1

# 调试配置
# 是否启用调试模式
DEBUG_MODE=true

# 是否保存交易日志
SAVE_TRANSACTION_LOGS=true

# 日志文件路径
LOG_FILE_PATH=./logs/transactions.log
